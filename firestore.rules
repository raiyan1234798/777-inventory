rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules for development - Allow all read and write operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
